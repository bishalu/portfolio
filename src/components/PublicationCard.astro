---
/**
 * PublicationCard Component
 * Display research paper/publication with clean academic styling
 */
interface Props {
  title: string
  abstract: string
  year: number
  journal?: string
  doi?: string
  pdf?: string
  coauthors?: string[]
  tags?: string[]
}

const { title, abstract, year, journal, doi, pdf, coauthors = [], tags = [] } = Astro.props

// Format authors for display
const authorDisplay = coauthors.length > 0 ? `with ${coauthors.join(', ')}` : ''
---

<article class="publication-card">
  <header class="pub-header">
    <span class="pub-year">{year}</span>
    <h2 class="pub-title">{title}</h2>
    {journal && <p class="pub-journal">{journal}</p>}
    {coauthors.length > 0 && <p class="pub-authors">{authorDisplay}</p>}
  </header>

  <p class="pub-abstract">{abstract}</p>

  {
    tags.length > 0 && (
      <div class="pub-tags">
        {tags.map((tag) => (
          <span class="tag">{tag}</span>
        ))}
      </div>
    )
  }

  <footer class="pub-links">
    {
      doi && (
        <a href={`https://doi.org/${doi}`} class="pub-link" target="_blank" rel="noopener noreferrer">
          <span>DOI</span>
        </a>
      )
    }
    {
      pdf && (
        <a href={pdf} class="pub-link" target="_blank" rel="noopener noreferrer">
          <span>PDF</span>
        </a>
      )
    }
  </footer>
</article>

<style lang="scss">
  .publication-card {
    transition: border-color 0.2s ease;
    border: 1px solid var(--hairline-on-paper);
    border-radius: 1rem;
    background: var(--paper-2);
    padding: 1.5rem;

    &:hover {
      border-color: var(--crimson);
    }
  }

  .pub-header {
    margin-bottom: 1rem;
  }

  .pub-year {
    display: inline-block;
    margin-bottom: 0.5rem;
    color: var(--marigold-text);
    font-weight: 500;
    font-size: 0.75rem;
    letter-spacing: var(--tracking-label);
    font-family: var(--font-mono);
  }

  .pub-title {
    margin-bottom: 0.25rem;
    color: var(--foreground-color);
    font-weight: 600;
    font-size: 1.125rem;
    line-height: 1.4;
    font-family: var(--font-heading);
  }

  .pub-journal {
    color: var(--color-warm-gray);
    font-style: italic;
    font-size: 0.9rem;
  }

  .pub-authors {
    margin-top: 0.25rem;
    color: var(--color-warm-gray);
    font-size: 0.85rem;
  }

  .pub-abstract {
    // Clamp to 4 lines
    display: -webkit-box;
    margin-bottom: 1rem;
    -webkit-line-clamp: 4;
    color: var(--color-warm-gray);
    font-size: 0.9rem;
    line-height: 1.6;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .pub-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .tag {
    border: 1px solid var(--hairline-on-paper);
    border-radius: 999px;
    padding: 0.125rem 0.5rem;
    color: var(--ink-soft);
    font-size: 0.7rem;
    font-family: var(--font-mono);
  }

  .pub-links {
    display: flex;
    gap: 0.75rem;
  }

  .pub-link {
    transition:
      background 0.2s ease,
      color 0.2s ease;
    border: 1px solid var(--crimson-deep);
    border-radius: 999px;
    padding: 0.25rem 0.75rem;
    color: var(--crimson-text);
    font-weight: 500;
    font-size: 0.8rem;
    letter-spacing: var(--tracking-label);
    font-family: var(--font-mono);
    text-transform: uppercase;
    text-decoration: none;

    &:hover {
      background: var(--crimson-deep);
      color: var(--paper);
    }
  }
</style>
